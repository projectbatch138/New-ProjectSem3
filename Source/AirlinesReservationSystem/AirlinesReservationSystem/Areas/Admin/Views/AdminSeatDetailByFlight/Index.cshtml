@model IEnumerable<AirlinesReservationSystem.SeatDetailByFlight>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FlightId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SeatNumberId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SeatStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FlightId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SeatNumberId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SeatStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SeatDetailByFlightId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SeatDetailByFlightId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SeatDetailByFlightId })
        </td>
    </tr>
}

</table>
